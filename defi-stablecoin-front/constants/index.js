//Este fichero lo creamos solo para importar el abi.json y contractAddresses.json aqui, y luego exportarlo de una .
//!EXAMPLE
const abiDSCEngine = require("./abiDSCEngine.json");
const abiWETH = require("./abiWETH.json");
const abiUSDD = require("./abiUSDD.json");

module.exports = { abiDSCEngine, abiWETH, abiUSDD };
